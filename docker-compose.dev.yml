services:
  backend:
    build:
      context: .
      target: dev
    container_name: lorem-backend-dev
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - db

include:
  - docker-compose.db.yml